__pycache__/
*.py[cod]
.DS_Store
.env
results.json
# Written beside results.json by tools/record_run.py. Like results.json it is
# produced after the commit is frozen, so it is retained outside git, not in it.
results_provenance.json
data/catalog.jsonl
data/releases/
# Organizer-only code, private evaluation data, manifests, and build reports.
organizer/
secure/
# Internal provenance and release-audit working documents.
docs/audits/
docs/data_selection_audit.md
docs/participant_release_checklist.md
# Organizer-only pipeline tests.
tests/test_5core_builder.py
tests/test_organizer_pipeline.py
*.log
# Response caches for the optional hosted layers. The agent never writes these -- only a
# measurement harness that opts in by calling `flush()` -- but they hold conversation text
# and resolved customer values, so they must never be committed.
submission/.llm_*cache*.json
