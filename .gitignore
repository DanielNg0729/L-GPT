# --- BTC data: quá lớn cho git, lấy lại bằng `cd provided && ./fetch.sh` ---
provided/release/*.gz
provided/release/*.zip
provided/techjam-conversational-search/data/catalog.jsonl

# --- secrets: TUYỆT ĐỐI không commit (vi phạm rule cuộc thi) ---
.env
.env.*
!.env.example
*.key
*.pem
secrets.json

# --- python ---
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
*.egg-info/

# --- output của agent / evaluator ---
results.json
runs/
*.log

# --- artifact nặng tự sinh ---
*.faiss
*.npy
*.index
cache/
.embeddings/

# --- macOS / editor ---
.DS_Store
.vscode/
.idea/

# --- tài liệu gốc có bản quyền của BTC (Early Bird Access) ---
# Deliverable yêu cầu repo PUBLIC -> không đưa vào history để khỏi phải rewrite sau.
# Bản tóm tắt tự viết (*.md) thì vẫn commit bình thường.
TikTok_TechJam_2026_Tracks_and_Problem_Statements.pdf
TikTok_TechJam_2026_Tracks_and_Problem_Statements.html
!.env.example
